{
  "name": "ai-disease-prediction-system",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "rm -f package-lock.json && rm -f frontend/package-lock.json && cd frontend && npm install && npm run build"
  }
}
