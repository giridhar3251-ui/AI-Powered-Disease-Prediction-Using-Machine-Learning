{
  "name": "ai-disease-prediction-system",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "npx rimraf package-lock.json frontend/package-lock.json && cd frontend && npm install && npm run build"
  }
}
